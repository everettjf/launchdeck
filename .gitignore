# XcodeGen generates LaunchDeck.xcodeproj from project.yml — do not commit it
LaunchDeck.xcodeproj/

# SwiftPM build artifacts for the local Core package
Core/.build/

.DS_Store

# SwiftPM build artifacts for the root package
.build/
